let hero = ['Batman', 'Superman', 'Spider', 'Flash', 'Black Phanter']

function forEach(indice = 0){
    indice += 1
}

hero.forEach(function(mome, indice) { 
   console.log(`${indice + 1} ${mome}`) 
})